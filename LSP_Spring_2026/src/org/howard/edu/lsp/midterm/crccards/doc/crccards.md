TaskManager collaborates with Task because the responsibilities of storing tasks, adding new tasks, finding a task by ID, and returning tasks by status each directly involve Task objects. Without the Task class, its not possible to store, search, or filter tasks. However, Task does not collaborate with TaskManager because the responsibilities of storing task information, updating task status, and providing task details are all self-contained behaviors that only need to know about themselves.